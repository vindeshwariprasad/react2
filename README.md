# React + Vite
Online Library System
A simple online library system built with React, Redux, and Vite. It allows users to browse books, add new books, view details, and filter books by category.

Getting Started
1. Clone the repository:

2. Install dependencies:
Using npm:

npm install
3. Start the development server:
Using npm:

npm run dev

